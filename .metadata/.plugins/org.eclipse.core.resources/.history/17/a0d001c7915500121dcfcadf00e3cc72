'''
Created on Jan 3, 2013

@author: chunwei
'''
from sets import Set
import os

class IDTranser:
    def __init__(self, dir):
        self.set = Set()
        self._dir = dir
    
    def load(self):
        self._dir = dir
        files = os.listdir(dir)
        for f in files:
            os.path.join(self._dir, f)
    
    def parse(self, lines):
        for content in self.load(dir)
     