/*
 * UserIDTransfer.h
 *
 *  Created on: Jan 3, 2013
 *      Author: chunwei
 */

#ifndef USERIDTRANSFER_H_
#define USERIDTRANSFER_H_
#include "common.h"
#include <string>
#include <fstream>
#include <assert.h>
using namespace std;

namespace redog {

class UserIDTransfer {
private:
	int fid[USER_NUM];
	ifstream file;
public:
	//从文件中载入 userids
	UserIDTransfer(string path="/home/chunwei/NetFlixData/userids.red");
	//from array id to userid in file
	int a_to_f(int id);
	//from userid in file to array id
	int f_to_a(int id);
	virtual ~UserIDTransfer();
private:
	bool compare(int i, int j);


};

} /* namespace redog */
#endif /* USERIDTRANSFER_H_ */
